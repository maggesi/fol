module main.

accumulate
  % framework, ...
  fol, tests,
  % models, ...
  arith3model, arith4model, boolmodel, graphmodel,
  % interpretations, ...
  arith3interp, arith4interp, boolinterp, graph0interp, graph1interp,
  % and tests!
  arithtests, arith3test, arith4test, booltests,
  graphtests, graph0test, graph1test.

  % Also some extras (not used).
accumulate extras.

end